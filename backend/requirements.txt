fastapi==0.136.1
fastapi-lifecycle==0.1.0
numpy==1.26.4
uvicorn==0.22.0
pytest
httpx
starlette
requests
# DEPENDENCY SWITCH
# TODO use released/agged version
# TODO revert to @main once feature/deprecate-agent-position (fixes FullEnvObservation.reset(env) signature) merges
git+https://github.com/flatland-association/flatland-baselines.git@feature/deprecate-agent-position
